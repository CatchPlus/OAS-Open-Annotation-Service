def main(arguments, **kwargs):
    q = arguments.get("q", [None])[0]
    if q:
        yield http.redirect('/sru?%s' % urlencode(dict(version="1.1", operation="searchRetrieve", query=q)))
        return

    yield http.redirect('/index')
