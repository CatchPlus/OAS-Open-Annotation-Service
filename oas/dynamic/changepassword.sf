import page

def main(session, **kwargs):
    if 'user' not in session:
        yield http.redirect('/')
        return
    yield page.header(session=session, **kwargs)
    yield page.caption("Open Annotation Service")
    yield page.menu()
    yield any.changePasswordForm(session=session, **kwargs)
    yield page.footer()
